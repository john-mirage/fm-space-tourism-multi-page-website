const componentTemplate = document.createElement("template");

componentTemplate.innerHTML = `
  <audio-view class="view" id="view"></audio-view>
  <audio-footer class="footer"></audio-footer>
`;

export default componentTemplate;
